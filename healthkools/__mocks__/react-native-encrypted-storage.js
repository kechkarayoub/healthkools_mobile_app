// __mocks__/react-native-encrypted-storage.js

// Mock the functions or methods you need for testing
const RNEncryptedStorage = {
    setItem: jest.fn(),
    getItem: jest.fn(),
    removeItem: jest.fn(),
    // Add any other methods you need to mock
  };
  
  export default RNEncryptedStorage;